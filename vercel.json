{
  "version": 2,
  "builds": [
    {
      "src": "App/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist" }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/App/dist/$1"
    }
  ]
}
